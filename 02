# Напишите программу, которая принимает на вход вещественное число и показывает сумму его цифр
# a = float (input ('Введите число: '))
a = list (input("Введите вещественное число: "))
a.remove('.')
b = list(map(int, a))   # Перевод строковых элементов в целые числа
s = 0
x = len (b)
count = 0
while count < x:
    s = s + b[count]
    count = count + 1
print ('Сумма цифр числа: ', s)




#Напишите программу, которая принимает на вход число N и выдает набор произведений чисел от 1 до N.
a = int (input ('Введите челое число: '))
print ('Последовательность произведений: ')
x = 1
for i in range (1, a + 1):
    x = i * x
    print (x)




# Задайте список из n чисел последовательности (1 + 1/n)^n и выведите на экран их сумму.
a = int (input ('Введите число: '))
i = 1
sum = 0
for i in range (1, a + 1):
    k = round ((1 + 1 / i)**i, 2)
    print (k, end=' ')
    sum = sum + k
print ()
print ('Сумма последовательности: ', sum)  




# Задайте список из N элементов, заполненных числами из промежутка [-N, N].
# Найдите произведение элементов на указанных позициях. Позиции хранятся в файле file.txt в одной строке одно число.
n = int (input ('Введите число: '))
x, y = map(int, input('Введите индексы: ').split( ))  # ВВод двух параметров через пробел
listA = [i for i in range (-n, n + 1)]
print (listA)
a = listA[x]
b = listA[y]
print ('Произведение выбранных чисел: ', a*b)




# Реализуйте алгоритм перемешивания списка
n = int (input ('Сколько элементов вы будете вводить: '))
a = [0] * n
for i in range (n):
    a[i] = int (input ()) 
print ('Ваш список: ')
print (a, end = ' ')
import random
length = len(a)
for i in range(0, length):
    rnd = random.randint(0, length - 1) # Рандомное перемешивание индексов
    temp = a[i]
    a[i] = a[rnd]
    a[rnd] = temp
print()
print ('Теперь список перемешан: ')
print (a)
